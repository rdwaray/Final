{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "job": {
      "type": "string"
    },
    "id": {
      "type": "string",
      "description": "An identifier for the job or role. May be absent in some cases."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the object was created. May be absent in some cases."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the object was last updated. May be absent in some cases."
    }
  },
  "required": ["name", "job"],
  "additionalProperties": false
}
